PORT=3000

# MySQL — create DB and tables: mysql -u root -p < db/schema.sql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rafiki_help_me

# Used to sign login tokens — use a long random string in production
JWT_SECRET=replace-with-a-long-random-secret
